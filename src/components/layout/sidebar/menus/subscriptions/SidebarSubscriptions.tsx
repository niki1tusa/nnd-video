
export const SidebarSubscription = () => {
    return (
        <div>
            <ul>
                {/* {menu.map(item => (
                    <MenuItem key={item.label} item={item} />
                ))} */}
            </ul>
        </div>
    );
};
